# 整改方向

## 审阅范围和判定方式

本清单以当前工作区可执行源码为准，核对了 `app/`、`frontend/src/`、`tests/`、`scripts/`、运行配置、提示词注册表和 CI。`logs/`、`.vite/`、`__pycache__/`、完整访谈转录与依赖锁文件属于生成物或历史输出，不作为业务实现依据。

当前项目存在两套互相覆盖的访谈实现：

1. **实际主链路**：高光访谈 -> LLM 生成大纲 -> 发布 -> 规则驱动的采集点文本收集。
2. **遗留链路**：开场/卡片 -> S1-S7 -> LangGraph 父子图 -> LLM 阶段总结。

`InterviewStateMachine.handle_dialog_text()` 在采集点校验后直接 `return await self._handle_outline_turn(...)`，使其后旧链路不可达。以下问题据此排序，不假设旧代码仍在生产中工作。

## P1：应优先处理

### 1. 公开的旧对话接口无法完成正式采访

- 依据：[app/services/interview_state_machine.py](app/services/interview_state_machine.py) 的 `handle_dialog_text()` 在采集点校验失败时直接返回提示，在其后对 `interview_graph.run_turn()` 的调用之前已经 `return`。
- 影响：`POST /interview/dialog/start`、SSE 开场和“直接开始采访”卡片仍然可调用，但第一次文本输入不会进入 S1-S7，而是要求先创建并发布大纲。API 契约、保留前端代码、模拟脚本和真实运行行为不一致。
- 整改：选择唯一产品主流程。若采集点流程为正式方案，移除或下线旧接口、卡片、阶段图和对应提示词；若要保留阶段采访，必须将其接回明确的路由分支，并为两套会话使用不同的状态模型与 API 前缀，避免一个 `handle_dialog_text()` 承担两种互斥语义。

### 2. 每个请求都会构建一整套未使用的 LangGraph

- 依据：[app/api/v1/routes/interview_dependencies.py](app/api/v1/routes/interview_dependencies.py) 每次依赖注入都新建 `InterviewStateMachine`；构造函数继续创建 `InterviewAgentService`、父图以及 S1-S7 的全部子图和 Redis checkpointer。
- 影响：当前采集点主链路每一次收发消息都支付阶段图构建成本，却不执行阶段图。请求开销、对象数量和排障面都被无效扩大。
- 整改：删除遗留图后将其从构造函数移除；若短期并存，将工作流对象放入 `app.state` 并按会话类型延迟创建。依赖函数只应返回已构造的服务或轻量请求上下文。

### 3. CI 与测试不代表当前可执行主链路

- 依据：`tests/test_interview_flow_rules.py` 有 57 个测试，主要断言 S1-S5/S1-S7 阶段图、旧提示词和已不可达的状态机方法；多个断言仍期待 S1 有 8 个问题、S5 是最终阶段，与当前注册表（S1/S7 分别为 5 个问题）不符。
- 依据：`tests/test_biography_flow.py` 和 `tests/test_highlight_interview.py` 创建临时目录但仍连接硬编码 PostgreSQL URL，没有使用临时目录或测试数据库；高光测试在关闭会话时也没有 mock 大纲模型调用。
- 依据：[.gitlab-ci.yml](.gitlab-ci.yml) 的 `test-backend` 只声明 Redis service，没有 PostgreSQL service 或 `BIOGRAPHY_DATABASE_URL`；同时 `bandit`、`ruff`、`compileall` 均引用不存在的 `demo` 目录。
- 影响：CI 不能可靠证明当前采集点链路；测试可能连到开发库，或因缺少 PostgreSQL/模型配置/路径而失败。
- 整改：以主链路重建 API 集成测试，测试数据库使用隔离的 PostgreSQL schema 或容器；mock `interview_llm.chat_json`；在 CI 注入 Redis 和 PostgreSQL；移除 `demo` 路径或恢复目录。旧链路若暂不删除，应移到单独的兼容测试组，并明确不属于主回归门禁。

### 4. 传记和素材接口没有身份认证或资源授权

- 依据：所有传记、会话、大纲与素材接口仅依赖路径中的 ID；`BiographyStore` 只保存可选的字符串 `interviewee_id`，没有账户、访问者或所有权校验。
- 影响：知晓或猜到 UUID/session ID 的调用者可以读取大纲、归档素材和状态，且可写入他人的传记会话。这些数据属于高敏感人生叙事内容。
- 整改：在路由层接入身份认证；为 biography、outline、session、material 增加 owner/tenant 字段和授权检查；禁止由客户端任意传入可代表他人的 `interviewee_id` 作为唯一边界。

## P2：功能正确性和架构收敛

### 5. 高光访谈的槽位覆盖会被助手提问污染

- 依据：[app/services/highlight_interview.py](app/services/highlight_interview.py) 将 `messages` 中用户和助手的全部 `content` 拼接为 `combined` 再评估。
- 影响：初始助手消息和后续固定问题含有“发生、什么时候、什么地方、哪些人”等关键词，会被 `CollectionPointEvaluator` 当作用户已提供的事实，提前提高覆盖率并可能过早生成大纲。
- 整改：只将用户消息组成评估文本；槽位值应包含来源回合/原文，不能仅以词命中标记为已采集。

### 6. 覆盖评估是脆弱的关键词命中，且实际采访不使用 LLM

- 依据：[app/services/collection_point_evaluator.py](app/services/collection_point_evaluator.py) 使用“在、年、他、她、后来”等子串判断槽位；[app/services/interview_state_machine.py](app/services/interview_state_machine.py) 的采集点回合按第一个缺失槽位返回固定文案。
- 影响：普通文本很容易误命中地点、人物或时间；不同用户表达方式会被漏判。采集点的标题、`hook` 和 `target_slots` 不能产生针对性动态追问，用户体验与“智能采访”定位不符。
- 整改：短期至少使用词边界、正则和明确的事实抽取结构，记录每个槽位的证据；中期用结构化 LLM 抽取和规则校验的组合。采集点生成应将当前采集点、已收集事实、缺失槽位和最近上下文传给模型，模型只给候选问题，代码保留关闭与持久化决策权。

### 7. 高光访谈没有最大回合保护，主动收束也可能生成贫乏大纲

- 依据：`HighlightInterviewService.handle()` 只有主动关键词或“至少两轮且覆盖阈值满足”才结束，没有最大轮数；主动说“说完了”会无条件生成大纲。
- 影响：低信息用户若不说结束会停留在无限固定追问；过早说结束又会把不足材料连同助手文本传给大纲模型。
- 整改：引入明确的最小事实要求、最大回合数和“继续/生成草稿/暂存”用户选择；不足时生成可编辑的有限草稿并在状态中标记 `insufficient_material`。

### 8. 采集点会话的手动完成与状态信息在 UI 中不可达

- 依据：[frontend/src/App.vue](frontend/src/App.vue) 将信息页按钮写为 `v-if="false"`；`InterviewStageBanner` 的“完成本段”按钮、`UserIdBar`、`StatusPanel` 和 `UserInfoPanel` 均只在该页内。
- 影响：前端虽然定义 `completeCollectionPoint()` 和命令接口，用户无法手动推进采集点或查看当前采集点；只剩后端关键词/覆盖率自动收束。
- 整改：将采集点标题、完成/跳过、当前覆盖和错误状态放到可见聊天页，或删除未交付的命令与组件。不要保留不可访问的功能入口。

### 9. 旧前端功能和当前首屏相互矛盾

- 依据：`App.vue` 默认页是 `outline`，没有 `onMounted` 调用 `resetDialog()`；聊天头部传入 `show-actions=false`；`useOnboarding()` 被实例化但从未调用 `start()`；旧卡片、开场流、恢复、用户资料代码仍保留。
- 影响：旧聊天页可手工切换但没有可见的新建会话入口，导览永远不显示，用户资料页不可达。代码体积和维护分支持续增加。
- 整改：采集点方案下移除旧开场/卡片/导览/资料页面，或为其建立独立、可达的产品入口。删除无调用的 composable、常量、API 方法和组件后再补针对当前页面的端到端测试。

### 10. 业务状态没有明确的所有权和恢复语义

- 依据：Redis 的会话状态只保存状态字符串；`InterviewContext` 的时间和 `previous_state` 在读取时重新生成。`DialogStartRequest.user_id` 也没有绑定到 session；用户资料仅在旧开场 SSE 中读取。
- 影响：恢复后无法准确得知上一个状态或会话归属；用户资料不参与高光/采集点流程，存在同一会话由任意用户 ID 操作的风险。
- 整改：定义 `InterviewSession` 持久化模型，至少保存 owner、biography、outline、状态、创建/更新时间和版本号；Redis 仅作缓存/短期检查点。所有回合 API 从服务端会话获取所属资源，不能信任客户端重复传参。

### 11. 默认会话 ID 可预测，重新开始会丢弃未归档的会话上下文

- 依据：`POST /interview/sessions` 在未传 `session_id` 时使用 `session:{biography_id}:{outline_id}`；`start_outline_session()` 随后调用 `_reset_interview_progress()`，删除同 ID 的 Redis 消息、进度和 LangGraph checkpoint。
- 影响：同一传记和大纲第二次点击“开始采集点采访”会覆盖正在进行的会话，而不是恢复；已写入 PostgreSQL 的 `material_fragments` 不会删除，造成“素材已归档、会话重新从第一采集点开始”的不一致。高光会话同样只在 Redis 中保存，TTL 到期后不可恢复。
- 整改：显式区分“新建”“继续”“重新开始”；新建使用随机 session ID，继续只读取不重置，重新开始应二次确认并保留可审计的历史版本。将高光会话和采集进度持久化，或清晰提示其过期时间。

### 12. 数据库访问在 async 请求中同步执行，且以 `create_all()` 代替迁移

- 依据：`BiographyStore` 使用同步 SQLAlchemy `Session`；`start_highlight_session`、`send_highlight_message`、`start_outline_session` 和文本回合都可能在 async 路径直接执行数据库操作。`app/main.py` 每次启动调用 `Base.metadata.create_all()`。
- 影响：数据库慢时会阻塞事件循环；表结构演进没有版本、回滚或部署顺序保障。
- 整改：选择 SQLAlchemy async engine，或将同步仓储调用显式放入线程池；使用 Alembic 管理 schema；上线前将建表从应用启动路径移到迁移任务。

### 13. 版本号生成存在并发竞争

- 依据：`BiographyStore.save_outline()` 通过 `max(version) + 1` 计算下一个版本，再插入具有唯一约束的记录。
- 影响：同一传记并发生成草稿时可能计算到相同版本，触发唯一约束异常。
- 整改：使用数据库事务锁、数据库序列，或在 `Biography` 上维护并原子递增当前版本；将完整性异常转换为可理解的业务错误。

## P3：冗余、规范和可维护性

### 14. 旧阶段图、状态机方法、提示词与测试构成大面积死代码

- 依据：旧图的唯一生产调用位于 `handle_dialog_text()` 的不可达 return 之后。`InterviewStageRuntime`、`InterviewParentGraph`、`InterviewStageSubgraph`、`InterviewAgentService` 的路由/主问题/总结方法、S1-S7 进度辅助方法以及相关 Redis checkpoint 在当前采集点回合中不执行。
- 依据：提示词注册表指向 `founder-s1` 至 `founder-s7`，同时 `app/prompts/interview/` 还保留另一套 `s1-childhood` 至 `s5-closing` 文件；`.codex/skills/` 再保留两套过时的提示词路由规则。
- 影响：同一概念有多个阶段数量、名称和问题数量，测试已经产生矛盾。维护者无法从目录判断哪套是生产逻辑。
- 整改：为旧模块设定明确期限。推荐将其移到独立分支或 `legacy/`，随后删除；如必须保留，补独立入口、集成测试和 README，禁止与采集点状态机混放。

### 15. `InterviewStateMachine` 同时承担两代产品的全部职责

- 依据：该文件约 1,750 行，包含 Redis 原语封装、会话状态机、旧阶段流程、采集点流程、岔题栈、归档、进度迁移和展示 DTO 适配。
- 影响：任何一项改动都可能影响另一条链路；不可达代码难以被发现；同名 `progress` 同时承载阶段与采集点语义。
- 整改：按边界拆为 `LegacyInterviewService`（若保留）、`OutlineInterviewService`、`SessionRepository`、`ConversationRepository`、`DiversionService`。为采集点进度建立独立 schema，避免混入 `stage_id/active_main_question_id` 等遗留字段。

### 16. 注释和命名未反映当前代码状态

- 依据：[app/graphs/interview_parent_graph.py](app/graphs/interview_parent_graph.py) 混有无缩进/无空行中文注释、`langgrpah` 拼写、`TODO：不知道在干什么` 等非说明性注释；`start-redis.bat` 实际启动 PostgreSQL 与 Redis；`watch-logs.ps1` 查找根目录日志，而模拟脚本输出到 `logs/`。
- 影响：注释不能解释设计约束，脚本名和真实行为不一致，排障会被误导。
- 整改：删除无效注释，保留解释状态约束或数据所有权的注释；重命名脚本或调整行为；为日志路径设统一配置并由启动脚本实际重定向输出。

### 17. 前后端阶段展示仍停留在旧 S1-S5

- 依据：后端阶段配置和提示词注册表为 S1-S7；`frontend/src/constants/interviewUi.js` 仍只展示 S1-S5 且名称不同。
- 影响：即使将来重新启用阶段图，前端也会对 S6/S7 回退为默认标题，且 S1-S5 文案不一致。
- 整改：删除旧阶段展示，或从后端统一下发阶段元数据，禁止前后端各自维护阶段定义。

### 18. 生成物和异常字节进入工作区

- 依据：仓库内存在 `frontend/.vite/`、`app/__pycache__/`、`tests/__pycache__/`、大量接口模拟日志和完整转录；`.gitignore` 末尾有含 NUL 字节的异常模式。
- 影响：生成物干扰代码检索、漏洞扫描、提交审阅和仓库体积；异常 `.gitignore` 规则不可预测。
- 整改：清理并从版本控制移除生成物和日志，补充 `frontend/.vite/`、`logs/*.json`、`logs/*.md` 等规则；以文本方式重写损坏的 `.gitignore` 行。需要保留的示例转录应移动到明确的 `fixtures/`，并做脱敏。

## 建议收敛顺序

1. 决定唯一正式业务流并删除/隔离另一条链路，先消除 `handle_dialog_text()` 的不可达块。
2. 为“高光 -> 大纲 -> 发布 -> 采集点 -> 归档”补完整的 PostgreSQL + Redis 集成测试和前端端到端测试，修复 CI 服务与无效路径。
3. 解决身份认证、资源授权和会话归属，再处理数据迁移与版本并发。
4. 用结构化事实抽取替换助手消息污染和关键词覆盖；让采集点追问真正使用当前上下文。
5. 拆分状态机、删除死代码与生成物，最后统一文档、脚本、命名和阶段元数据。

## 本次验证说明

已完成源代码、配置、测试和脚本的静态交叉核对。`python -m compileall -q app tests scripts` 通过，`git diff --check` 通过。当前默认 Python 解释器为 LibreOffice 自带 Python，未安装项目依赖，执行 `python -m unittest discover -s tests -v` 在导入 `pydantic` 时失败；这只说明本机未使用项目虚拟环境，不能替代项目测试结论。

前端方面，依赖包已可见，但本机 npm 没有生成 `node_modules/.bin/vite` shim；直接调用 Vite CLI 又在加载配置时被 `esbuild` 的 `spawn EBUSY` 阻断。因此本次不能宣称前端生产构建通过，该结果属于当前宿主环境的进程启动限制，仍需在标准 Node 22 CI 容器中复验。CI/测试自身的结构性问题已在第 3 项列出，整改后应在隔离 PostgreSQL、Redis 和 mock 模型条件下重新执行完整回归。
