# 启动说明

## 一、启动后端

在 VS Code 终端中，确保在项目根目录 `D:\simple`：

```powershell
# 如果当前在 D:\simple\app
cd ..

# 激活虚拟环境（如果没激活）
.venv\Scripts\activate

# 启动后端
fastapi dev app/main.py --host 127.0.0.1 --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 即启动成功。

---

## 二、启动前端

在 VS Code **新开一个终端窗口**（别关后端）：

```powershell
cd D:\simple\frontend

# 第一次启动需要装依赖，之后不需要
npm install

# 启动前端开发服务器
npm run dev
```

启动成功会输出：
```
VITE v5.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
```

---

## 三、访问

| 后端接口文档 | 前端页面 |
|---|---|
| http://127.0.0.1:8000/docs | http://localhost:5173 |

---

## 启动前确认

| 检查项 | 说明 |
|---|---|
| 虚拟环境已激活 | 终端前面有 `(.venv)` 标识 |
| Redis 在运行 | `redis-cli ping` 返回 `PONG` |
| `.env` 文件存在 | `D:\simple\.env`，里面配好了 `DASHSCOPE_API_KEY` |