# 7 天线下协同传记采访实现方案

## 概要

产品定位为“老人账号自主拥有采访资料、线下辅助者陪同操作”的采访智能体。每位老人使用自己的账号登录；线下辅助者只在现场协助老人录入和使用设备，不拥有系统账号、权限或专属功能。系统负责采访节奏、提问、阶段推进、情绪承接、材料抽取、矛盾核验和长期保存。传记成稿生成不在本项目实现；本项目向后续生成系统交付版本化素材包 API。

正式流程：

```text
创建采访
-> 高光破冰
-> AI 生成完整采访大纲
-> 手动修改并确认大纲
-> 6 天逐阶段采访
-> 第 7 天事实核验与补访
-> 发布版本化素材包
```

默认日程：第 1 天高光破冰加第一阶段；第 2-6 天各完成一个阶段；第 7 天集中核验矛盾、补足缺口并发布素材包。实际会面中断时，可从任意阶段和采集点长期恢复。

## 阶段与采集点

以“六个人生域硬约束 + 高光驱动的 AI 完整大纲 + 手动修改确认”的方式解决不能全预设、也不能完全无约束地由 AI 生成的问题：

- 系统内置 6 个不可删除的人生域约束，但不提供固定的阶段标题和采集点。AI 必须生成与这 6 个域一一对应的 6 个阶段，每阶段生成 2-3 个采集点。
- 高光破冰围绕“最自豪、最难忘或最想留下的一件事”展开，分配 1 个开场问题和最多 3 个追问；在核心信息已足够或问题额度用尽时收束，再生成大纲。
- AI 输出完整结构化大纲：人生域、阶段标题、阶段目的、2-3 个采集点、开场提问意图、低压力提问意图、自然语言材料目标、收束规则和与高光的关联证据。提问意图是供实时 LLM 生成问句的上下文，不能被客户端直接照读。服务端校验六域齐全、每阶段采集点数量和高光证据引用。
- 老人登录后的大纲编辑页支持修改阶段标题、采集点、顺序、提问意图和材料目标，也支持删除或补充采集点；确认后形成不可被模型静默改写的“采访计划版本”。现场辅助者可在同一老人账号会话中代为操作，但不形成独立身份或权限记录。

| 人生域 | AI 生成阶段必须覆盖的主题 |
| --- | --- |
| 1. 原生家庭与童年印记 | 成长环境与家庭日常；影响最深的人或童年经历 |
| 2. 求学与青春成长 | 学习、同伴或师长；第一次独立、兴趣或理想形成 |
| 3. 走入社会与职业起步 | 第一份工作或谋生经历；关键技能、选择或适应过程 |
| 4. 关系、家庭与责任 | 重要关系和支持；承担责任、照料或重要取舍 |
| 5. 人生转折与贡献 | 最重要的成就、困难或转折；对他人、行业或社区的贡献 |
| 6. 当下生活与人生回望 | 当前生活和珍视的人事；人生体会和想留下的话 |

每个采集点配置自然语言材料目标、开场和低压力提问意图、敏感边界、完成条件和问题额度。默认额度为 1 个开场问题、最多 2 个核心缺口追问、最多 1 个传记价值追问；每次调用模型都传入剩余可发问数和各类剩余追问数，额度用尽后只能收束或创建待复访任务。实际开场、追问、收束和换题文案均由当轮 LLM 生成。

## 采访状态机

- 会话状态统一为：`PREPARING`、`HIGHLIGHT`、`PLAN_CONFIRMATION`、`IN_PROGRESS`、`PAUSED`、`STAGE_REVIEW`、`DAY_7_REVIEW`、`MATERIAL_READY`。
- 采集点有两套独立状态：对话状态 `NOT_STARTED / ACTIVE / SOFT_COMPLETE / PAUSED / SKIPPED` 由实时链路维护；材料状态 `PENDING_EXTRACTION / VERIFIED_COMPLETE / NEEDS_REVISIT / NEEDS_REVIEW` 由后台维护。阶段只有全部采集点对话收束，且后台任务完成、失败或标为待人工复核后才能结束。
- 每个用户回合先持久化老人账号会话中录入的原话和后台处理任务；材料抽取随后由后台异步执行。每项材料必须保存来源回合和原文证据摘录；无法由原话支撑的内容不得追加到材料账本。
- 后台材料抽取不强制套入时间、地点、人物等固定字段。LLM 每轮返回带原文证据的“新增信息补丁”，可按当前叙述动态描述人物、事件、关系、物件、感受或其他传记线索；后台先保存不可变补丁，再以 PostgreSQL `jsonb_set` 和 `||` 将验证后的新增项追加到采集点 JSONB 材料账本。实时链路只以采集点的自然语言采集目标判断当前对话是否继续或收束。
- 追加不是覆盖：每项已接收材料由服务端分配稳定 `material_id`，并保留其补丁、来源回合和原文证据。后续补充、澄清和纠正永远新增材料；采集点账本不更新或删除历史材料。
- 用户出现跳跃叙述时，路由器区分“完整的新故事”和“仅顺带提及”：前者暂停当前采集点，使用独立的最多 2 次临时追问额度优先跟随；后者只创建待回访线索，不切换当前采集点。
- 跳跃故事与既有阶段匹配时，直接归入该阶段及相应采集点；无法匹配时归为“未计划片段”，保存后由阶段收束时决定是否转为补点。临时故事额度用尽后恢复原采集点；原采集点额度已用尽则直接进入下一次选题。
- 新人物、地点、组织、物件和事件建立实体及引用关系。仅当实体与当前故事的关系、作用或结果缺失时，生成一次实体相关追问；每回合最多一个问题。

### 实时回复与后台事实处理分离

- **实时回复链路**只读取当前采集点、最近对话、时间和轻量完成度，判断哪些信息维度已谈到、哪些待采集，并返回一个状态 `CONTINUE`、`DEEPEN`、`WRAP`、`SWITCH` 和一条下一问。它不等待事实抽取、不写入事实、不做前后矛盾校验。
- 实时链路中的语义判断和发问全部由 LLM 通过结构化输出完成：信息维度覆盖、低信息、拒答、敏感情绪、跳跃故事、目标人生域、下一动作和下一问都不得用正则、关键词、字符包含或字符串长度规则判断。后端仅校验模型动作是否符合当前状态、问题额度、权限和数据完整性，不根据老人原话自行路由。
- 所有用户可见的开场、追问、收束、换题和情绪承接均由 LLM 生成；服务端只向模型提供采访目标、对话上下文、剩余问题额度和允许动作，不拼接或返回固定采访文案。模型连续失败时，采访状态置为 `REPLY_PENDING` 并由前端提示重新生成，不以固定话术替代。
- **后台材料链路**异步消费已保存的原话，完成增量材料/实体抽取、证据子串校验、实体归档和一致性检测。后台结果只能更新材料状态、阶段缺口和待复访任务，不能中断或改写已发出的回复。
- 每个采集点同时保存两个状态：对话状态 `ACTIVE / SOFT_COMPLETE / PAUSED / SKIPPED`，材料状态 `PENDING_EXTRACTION / VERIFIED_COMPLETE / NEEDS_REVISIT / NEEDS_REVIEW`。`SOFT_COMPLETE` 仅表示当前对话可先切换，不代表材料已经完整可信。
- 后台发现关键材料缺口时，创建待复访任务。若当前点仍在进行，任务只在下一轮选题时参与决策；若当前点已收束，则在当前阶段末、下一次恢复或第 7 天复核时处理，绝不插入正在进行的用户回复。

### 采集点深度和切换节奏

- 每个采集点将目标分成三层：核心链路（事件、行动、结果，必须）；人物价值（人物、关系或影响，按点配置）；传记价值（感受、选择原因或人生意义，至少一项）。
- 标准提问预算为：1 个开场问题、最多 2 个核心缺口追问、最多 1 个传记价值追问和 1 个收束动作。老人一次回答足够丰富时，未使用的问题预算直接取消，不为凑轮次重复提问。
- 只有在当前采集点的自然语言材料目标仍有关键缺口、新内容能解释当前故事的关键人物或选择、或尚无传记价值信息时才 `DEEPEN`。是否材料已足够由 LLM 结合完整对话和剩余额度判断，不以“事件、行动、结果”等固定字段槽位判定。
- 已对同一缺口追问两次、用户低意愿/敏感、或当前采集点的可发问额度用尽时停止深挖并 `WRAP`，将剩余缺口标为待复访。
- 切换候选包括当前点必要缺口、当前阶段后台生成的待复访任务、未开始的计划采集点和跨阶段待回访线索。LLM 根据完整对话、采集目标、低意愿/敏感信号和问题额度选择候选并生成不新增材料的收束语与下一点首问；后端仅校验被选候选属于当前采访且状态允许，当前点的背景材料处理继续在后台完成。

## 情绪、低信息与事实核验

- 对“想不起来、不知道、没什么可说”等低信息回答，系统先简短安抚，再自动切换到当前阶段更容易回答的采集点；不连续逼问同一细节。
- 对明确拒答，停止当前话题，记录“当日跳过/不愿谈”的原因并转向低压力内容；只有老人后续主动提起时才再次展开。
- 对悲伤、焦虑和创伤叙述，系统只做轻量共情和安全换题，不提供心理咨询、不追问痛苦细节，并将该采集点标记为敏感。
- 每个阶段结束执行叙述一致性检查，而非外部真实性验证：LLM 从带原话证据的动态材料中识别难以同时成立的陈述，只对有明确证据冲突的内容生成中性确认问题。
- 老人可确认一种说法、补充或修正材料、保留两种不同情境，或标记“暂不确定”。未解决矛盾进入第 7 天复核；没有外部证据时不得由系统裁定真假。
- 一致性问题只引用材料 ID、来源补丁、原话回合和证据摘录，不修改材料账本。老人答复同样先保存为原话并异步追加补丁；后台再将该补丁与一致性问题关联为“确认 / 纠正 / 两种说法均保留 / 暂不确定”。素材包同时交付不可变原始材料和此解析视图，解析视图不回写或覆盖原材料。

## 数据、接口与工作台

- PostgreSQL 成为长期事实来源，保存：老人账号、采访、日程、六人生域约束、AI 大纲草稿与计划版本、采集点、回合原文、按回合保存的 LLM 信息补丁、按采集点 JSONB 追加的材料账本及证据、实体、跳跃线索、情绪/敏感标记、核验问题与答案、阶段总结、素材包版本。
- Redis 仅用于流式输出、短期锁和缓存，不保存唯一会话状态。旧 Redis 高光会话、旧大纲和 S1-S7 流程不迁移。
- 新接口包括：老人账号登录；创建/恢复采访；提交统一采访回合；读取当前日程、阶段、采集点剩余问题额度和待回访线索；读取、修改并确认 AI 大纲；完成阶段核验；读取并发布素材包版本。
- 素材包 API 固定返回交付结构而非固定事实字段：阶段、采集点、原话回合、增量材料补丁、材料账本、原文证据、实体引用、跳过项、敏感标记、一致性问题及其解析视图和版本号，供其他系统生成传记。
- 首版为每位老人提供一个账号：账号只能访问自己的采访及素材，不实现家属协作、辅助者后台或管理员后台。登录、会话和所有数据读写均按老人账号所有权校验。
- 前端改为老人采访工作台：今日阶段、当前采集点、剩余可发问数、原话记录、已提取事实、待回访线索、阶段核验和跨日恢复入口；高光后先展示可修改的 AI 大纲，不展示旧阶段号、隐藏完成按钮或旧大纲页面。

## 测试与迁移

- 单元测试：事实必须含来源证据、采集点完成判断、实体去重、跳跃故事跟随与恢复、低信息换题、拒答不重复追问、敏感内容不深挖、矛盾候选与确认结果。
- 集成测试：高光破冰生成、手动修改并确认完整大纲、连续 7 天恢复、六阶段完成、阶段末核验、素材包版本发布、老人账号之间的数据隔离。
- 端到端测试：老人账号创建采访、录入原话、编辑 AI 大纲、问题额度收束、跨阶段跳跃、自动低压力引导、核验纠偏和素材包读取。
- 使用数据库迁移建立新表和初始六阶段模板；移除或下线旧高光/大纲/采集点 API 与前端入口，避免双链路共存。
- LLM 调用全部使用结构化输出校验；后台抽取解析失败时保存原话并标记待人工复核；实时路由或问句生成解析失败时置 `REPLY_PENDING` 并允许重新生成，绝不以固定采访文案替代。

## 默认约束

- 首版仅支持老人账号会话中的文字录入；现场辅助者可协助输入，不支持录音、语音转写、照片、家属协作或传记成稿生成。
- AI 生成整体大纲后支持手动修改并确认；采访进行中不手工决定 AI 路由。
- 每个采集点按问题额度收束，不以时间作为切换条件；模型收到剩余可发问数和追问额度，额度耗尽后不得再生成新问题。
- 禁止在新采访链路中使用旧 `UnifiedSemanticRouter`、`CollectionPointEvaluator` 或任何以原文关键词/正则驱动的路由实现；旧链路下线后相关入口不可达。
- “事实校验”只校验用户叙述前后的一致性，不承担外部事实核查。
